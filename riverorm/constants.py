"""Constants used across the RiverORM library."""

# Database types
MYSQL = "mysql"
POSTGRES = "postgres"
SUPPORTED_DBS = {MYSQL, POSTGRES}
DEFAULT_DB = POSTGRES

# Default primary key field name
DEFAULT_PRIMARY_KEY = "id"
